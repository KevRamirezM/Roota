"""
accessibility — Windows UI Automation tree scanner and coordinate parser.

Reads active window elements (buttons, inputs, menus) and exposes them as
structured data for the orchestration layer.
"""
